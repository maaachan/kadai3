<% @books.each do |book| %>
<div>
  
  <p><%= book.user.name %></p>
  <%= link_to books_path(book.id) do %>
  <p>Title：<%= book.title %></p>
  <% end %>
  <p>Opinion：<%= book.body %></p>
</div>

<% end %>
